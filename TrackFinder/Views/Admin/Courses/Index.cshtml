@model IEnumerable<TrackFinder.ViewModels.Courses.CourseListViewModel>
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<div class="d-flex justify-content-between align-items-center mb-3">
    <h3>Courses</h3>
    <a class="btn btn-primary" asp-controller="Courses" asp-action="Create">New Course</a>
</div>

<table class="table table-hover bg-white shadow-sm rounded">
    <thead>
        <tr>
            <th>Name</th>
            <th>Instructor</th>
            <th>Price</th>
            <th>Level</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model ?? Enumerable.Empty<TrackFinder.ViewModels.Courses.CourseListViewModel>())
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.InstructorName</td>
                <td>@item.Price.ToString("C")</td>
                <td>@item.Level</td>
                <td class="text-end">
                    <a class="btn btn-sm btn-outline-primary me-1" asp-action="Details" asp-route-id="@item.Id">Details</a>
                    <a class="btn btn-sm btn-outline-secondary me-1" asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                    <a class="btn btn-sm btn-outline-danger" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>