@model string

<div class="tool-chip card border-0 rounded-pill">
    <div class="card-body px-3 py-2">
        <span class="tool-chip-label">@Model</span>
        @* TODO: tool descriptions are not exposed by the current TrackStackVM. Add a tool view model when the backend supplies descriptions. *@
    </div>
</div>
